<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Pulse - A better Discord</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(115deg, #0a0a0a 50%, #1e1e1e 50%);
      color: #fff;
    }
    .navbar {
      display: flex;
      justify-content: space-between;
      padding: 1rem 2rem;
      background-color: #1f1f1f;
      box-shadow: 0 2px 5px rgba(0,0,0,0.3);
      position: fixed;
      width: 100%;
      z-index: 1000;
    }
    .logo {
      font-size: 1.5rem;
      font-weight: bold;
      color: #7289da;
    }
    .nav-links {
      display: flex;
      gap: 1rem;
      margin-right: 3rem;
    }
    .nav-links a {
      margin-left: 0rem;
      text-decoration: none;
      color: #fff;
      padding: 0.5rem 1rem;
      border-radius: 5px;
      transition: background 0.3s;
    }
    .nav-links a:hover {
      background: #2a2a2a;
    }
    .hero {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      padding: 0 2rem;
      text-align: center;
    }
    .hero h1 {
      font-size: 3rem;
      margin-bottom: 1rem;
      background: linear-gradient(90deg, #7289da, #8593ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
    .hero p {
      font-size: 1.2rem;
      max-width: 600px;
      margin-bottom: 2rem;
      opacity: 0.8;
    }
    .hero .buttons {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
      justify-content: center;
    }
    .buttons a {
      padding: 0.75rem 1.5rem;
      border-radius: 5px;
      font-weight: 500;
      text-decoration: none;
    }
    .buttons .login {
      background: #2a2a2a;
      color: white;
      border: 1px solid #444;
      transition: all 0.3s;
    }
    .buttons .register {
      background: #7289da;
      color: white;
      border: 1px solid #647ab5;
      box-shadow: 0 0 15px rgba(112,137,210,0.3);
    }
    .buttons .login:hover {
      background: #3a3a3a;
    }
    .buttons .register:hover {
      background: #6372e9;
      box-shadow: 0 0 20px rgba(112,137,210,0.5);
    }
    footer {
      position: fixed;
      bottom: 0;
      width: 100%;
      text-align: center;
      padding: 1rem;
      background: #121212;
      font-size: 0.9rem;
      opacity: 0.7;
      color: #ccc;
    }

    body::-webkit-scrollbar {
      width: 4px;
    }

    body::-webkit-scrollbar-track {
      background: #1e1e1e;
    }

    body::-webkit-scrollbar-thumb {
      background: #7289da;
      border: none;
    }

    body::-webkit-scrollbar-thumb:hover {
      background: #6372e9;
    }
  </style>
</head>
<body>
  <nav class="navbar">
    <div class="logo">Pulse</div>
    <div class="nav-links">
      <a href="/login">Login</a>
      <a href="/register">Register</a>
    </div>
  </nav>
  <main class="hero">
    <h1>A better discord</h1>
    <p>
      Tired of discord banning you for child safety? Well fuck you discord because ima do my own thing.
    </p>
    <div class="buttons">
      <a href="/login" class="login">Login</a>
      <a href="/register" class="register">Create Account</a>
    </div>
  </main>
  <footer>
    © 2025 Playstation451 • All rights reserved
  </footer>
</body>
</html>