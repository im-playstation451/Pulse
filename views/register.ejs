<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Pulse - Register</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-gray-900 to-gray-800 font-sans text-white antialiased">
  <nav class="flex justify-between items-center px-6 py-4 bg-gray-800 shadow-lg fixed w-full z-50">
    <h1 class="text-2xl font-bold text-blue-600">Pulse</h1>
    <div class="flex gap-4">
      <a href="/login" class="px-4 py-2 bg-gray-700 rounded hover:bg-gray-600 transition">Login</a>
      <a href="/register" class="px-4 py-2 bg-transparent border border-blue-600 text-blue-600 hover:bg-blue-600 hover:text-white transition">Register</a>
    </div>
  </nav>

  <main class="flex items-center justify-center min-h-screen p-6">
    <div class="max-w-md w-full bg-black/30 backdrop-blur-md p-8 rounded-xl shadow-lg">
      <% const error = locals.error || ''; %>
      <% if (error) { %>
        <div class="text-red-500 text-center mb-4 font-semibold"><%= error %></div>
      <% } %>
      <h2 class="text-2xl text-center mb-6 text-blue-600 font-bold">Register for Pulse</h2>
      <form action="/register" method="POST" class="space-y-4">
        <div class="space-y-2">
          <input type="text" name="username" placeholder="Username" required class="w-full p-3 bg-gray-800 rounded-lg focus:bg-gray-700 outline-none transition" />
          <input type="email" name="email" placeholder="Email" required class="w-full p-3 bg-gray-800 rounded-lg focus:bg-gray-700 outline-none transition" />
          <input type="password" name="password" placeholder="Password" required class="w-full p-3 bg-gray-800 rounded-lg focus:bg-gray-700 outline-none transition" />
          <input type="password" name="repeatPassword" placeholder="Repeat Password" required class="w-full p-3 bg-gray-800 rounded-lg focus:bg-gray-700 outline-none transition" />
        </div>
        <button type="submit" class="w-full py-3 bg-blue-600 rounded-lg hover:bg-blue-500 shadow-md hover:shadow-lg transition duration-300">Register</button>
      </form>
      <div class="text-center text-sm mt-6">
        <p>Already have an account? <a href="/login" class="text-blue-600 hover:text-blue-500 transition">Login here</a></p>
      </div>
    </div>
  </main>

  <footer class="fixed bottom-0 w-full bg-gray-900 text-center py-3 text-gray-500 text-sm text-opacity-70">© 2025 Playstation451 • All rights reserved</footer>
</body>
</html>